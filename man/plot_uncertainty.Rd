% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_uncertainty.R
\name{plot_uncertainty}
\alias{plot_uncertainty}
\title{Plot prediction intervals}
\usage{
plot_uncertainty(
  intervals,
  actual = NULL,
  facet = TRUE,
  title = NULL,
  show_points = TRUE
)
}
\arguments{
\item{intervals}{A tibble returned by \code{\link[=predict_interval]{predict_interval()}}.}

\item{actual}{Optional numeric vector of observed outcomes aligned with the
\code{row} column in \code{intervals}.}

\item{facet}{Logical. If \code{TRUE} (default) a facet is drawn per method;
otherwise methods are distinguished via colour.}

\item{title}{Optional plot title.}

\item{show_points}{Logical controlling whether point predictions are drawn.}
}
\value{
A \code{ggplot} object.
}
\description{
Given the tidy output from \code{\link[=predict_interval]{predict_interval()}}, creates a faceted ribbon plot
displaying point predictions and corresponding uncertainty bands. Optionally
overlays observed outcomes for quick calibration diagnostics.
}
\examples{
mod <- lm(mpg ~ wt + hp, data = mtcars)
ufit <- fit_uncertainty(mod, method = "bootstrap", B = 100)
ints <- predict_interval(ufit, mtcars[1:10, ])
plot_uncertainty(ints, actual = mtcars$mpg[1:10])
}
