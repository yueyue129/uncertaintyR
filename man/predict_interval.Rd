% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_interval.R
\name{predict_interval}
\alias{predict_interval}
\title{Compute prediction intervals from an uncertainty fit}
\usage{
predict_interval(
  object,
  newdata,
  level = object$level,
  method = object$method,
  B = object$B,
  keep_draws = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An object returned by \code{\link[=fit_uncertainty]{fit_uncertainty()}}.}

\item{newdata}{A data frame containing predictors at which predictions should
be evaluated. Must include the same columns that were used to fit the
original model.}

\item{level}{Nominal coverage level. Defaults to the level stored in the
fit object.}

\item{method}{Subset of methods to evaluate. Defaults to all methods that
were enabled when \code{fit_uncertainty()} was called.}

\item{B}{Optional integer specifying the number of resamples to draw. If
omitted, the value stored inside the fit object is used.}

\item{keep_draws}{If \code{TRUE}, the function also returns the raw resampled
draws (useful for custom plotting). Defaults to \code{FALSE}.}

\item{seed}{Optional integer for reproducibility.}

\item{...}{Reserved for future extensions.}
}
\value{
A tibble with columns \code{row}, \code{method}, \code{estimate}, \code{lower}, \code{upper},
\code{std_error}, and optional metadata. When \code{keep_draws = TRUE}, an additional
list-column \code{draws} is included.
}
\description{
Given an object created by \code{fit_uncertainty()}, \code{predict_interval()} produces
uncertainty-aware predictions for new data using one or more resampling
strategies. Output is returned in a tidy tibble with one row per observation
per method.
}
\examples{
mod <- lm(mpg ~ wt + hp, data = mtcars)
ufit <- fit_uncertainty(mod, method = c("bootstrap", "jackknife"), B = 100)
predict_interval(ufit, newdata = mtcars[1:5, ])
}
