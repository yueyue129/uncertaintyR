% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_uncertainty.R
\name{fit_uncertainty}
\alias{fit_uncertainty}
\title{Fit an uncertainty wrapper around a predictive model}
\usage{
fit_uncertainty(
  model,
  X = NULL,
  y = NULL,
  method = c("jackknife", "bootstrap", "bayes", "conformal"),
  B = 200,
  level = 0.95,
  seed = NULL,
  ...
)
}
\arguments{
\item{model}{A fitted model object with valid \code{predict()} and \code{update()}
methods (e.g., objects returned by \code{lm()}, \code{glm()}, \code{nnet::nnet()},
\code{caret::train()}, etc.).}

\item{X}{Optional data frame or matrix of predictors used to fit \code{model}.
Required when the original training data cannot be reconstructed from the
fitted object (e.g., when the model stores a pre-processed design matrix
only). Must have the same number of rows as \code{y}.}

\item{y}{Optional numeric outcome vector aligned with \code{X}. If omitted,
\code{fit_uncertainty()} attempts to retrieve the response from \code{model}.}

\item{method}{Character vector of uncertainty methods to enable. Supported
choices are \code{"jackknife"}, \code{"bootstrap"}, \code{"bayes"}, and \code{"conformal"}.
Provide more than one value to enable multiple methods.}

\item{B}{Number of resamples or posterior draws used by methods that require
Monte Carlo sampling (bootstrap and Bayesian). Defaults to 200.}

\item{level}{Nominal coverage level used by conformal intervals. Individual
methods may compute quantiles directly in \code{predict_interval()}.}

\item{seed}{Optional integer seed to make resampling reproducible.}

\item{...}{Additional arguments reserved for future extensions.}
}
\value{
An object of class \code{"uncertainty_fit"} that records the fitted model,
training data, resampling configuration, and stored diagnostics.
}
\description{
\code{fit_uncertainty()} stores the training data, residual diagnostics, and
resampling configuration needed to compute uncertainty estimates for any
model that supports \code{predict()} and \code{update()}. The function does \strong{not}
refit the model; instead, it records the information required so that
intervals can be generated later with \code{predict_interval()}.
}
\examples{
mod <- lm(mpg ~ wt + hp, data = mtcars)
ufit <- fit_uncertainty(mod, method = c("bootstrap", "conformal"), B = 100)
ufit
}
